# Environment Configuration for 3D Printer Stream

# Application Configuration
# Port the application runs on (default: 3000 for serve)
PORT=3000

# Node environment (production, development)
NODE_ENV=production

# Session secret for any session management (generate a random string)
# You can generate one with: openssl rand -base64 32
SESSION_SECRET=your_session_secret_here

# Cloudflare Configuration (Optional - Shared across all domains)
# These credentials are used to purge Cloudflare cache after deployment
# Get your API token from: https://dash.cloudflare.com/profile/api-tokens
# Create a token with "Zone.Cache Purge" permissions
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here

# Get your Zone ID from: https://dash.cloudflare.com/ > Select your domain > Overview (right sidebar)
# This is the Zone ID for the domain you want to purge cache for
CLOUDFLARE_ZONE_ID=your_cloudflare_zone_id_here

# Note: Cloudflare credentials are optional. If not provided, cache purging will be skipped
# during deployment (non-fatal). The same credentials can be used across all your domains
# if you create a token with permissions for all zones.

